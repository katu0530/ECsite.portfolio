<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.ItemMapper">

	<select id="selectAll" resultType="com.example.domain.Item">
		SELECT * FROM items
	</select>

	<select id="selectById" parameterType="int"
		resultType="com.example.domain.Item">
		SELECT
		items.id, items,item_name, items.price,
		items.item_introduction,
		FROM items
		WHERE items.id = #{id}
	</select>

	<insert id="insert" parameterType="Item">
		INSERT INTO items (item_name,
		price, item_introduction)
		VALUES (#{itemName}, #{age}, #{address},
		#{itemIntroduction})
	</insert>

	<update id="update" parameterType="Item">
		UPDATE items
		SET name = #{itemName}, price = #{price},
		item_introduction = #{itemIntroduction}
		WHERE id = #{id}
	</update>

	<delete id="delete" parameterType="int">
		DELETE FROM items WHERE id = #{id}
	</delete>


</mapper>